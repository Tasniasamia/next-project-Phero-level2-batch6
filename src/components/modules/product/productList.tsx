"use client";

import { use } from "react";

const ProductList = ( slug: Promise<{slug:string[]}>) => {
    // const {slug}=use(params);
    // console.log("product data",slug);
    return (
        <div>
            Blog List Data
        </div>
    );
};

export default ProductList;